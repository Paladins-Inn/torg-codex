include::partial$config.adoc[]

.Level 1 – Übersicht des {system}-Systems
[plantuml, whitebox-torg-codex]
----
@startuml whitebox-torg-codex
skinparam componentStyle rectangle

actor "User / Player" as user

interface "DriveThruRPG API" as drivethru
database  "PostgreSQL"        as db

rectangle "torg-codex" <<SCS, Spring Boot>> {

  component "torg-codex\n(Frontend)" <<Spring Boot, JTE>> as frontend {
    interface "Web UI"   <<JTE, HTML>> as webui
    interface "REST API" <<JSON>>      as restapi
  }

  component "torg-codex-data\n(Data Layer)" <<Spring Boot, JPA>> as data {
    component "DriveThruRPG\nClient"    as dtclient
    component "Security &\nUserService" as security
    component "Markup\nGenerator"       as markup
    component "Data Model\n& Repository" as model
  }

  frontend -down-> data : uses
  security -right-> dtclient : authenticates via
  model    -down-> db        : persists via JPA
}

user       -right-> frontend  : HTTP / REST
dtclient   -down->  drivethru : REST (HTTPS)

@enduml
----
